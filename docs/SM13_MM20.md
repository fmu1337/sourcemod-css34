# sm13-mm20 — Metamod 2.0 (pre-KHook) on CS:S v34

Experimental line: **SourceMod 1.13.0.7404** + **Metamod:Source 2.0 git1407** (`0084b86…`).

Release tag: [`1.13.0.7404-mm2.0.0`](https://github.com/fmu1337/sourcemod-css34/releases/tag/1.13.0.7404-mm2.0.0).

```bash
CSS34_LINE=sm13-mm20 PURE_SOURCE_BUILD=1 builder/docker/legacy-build.sh
```

KHook / MM 2.0 git1467+ experiments live on branch `cursor/mm20-khook-c33d` (PR #54), not in this line.
The native KHook SM port is tracked separately on `claude/khook-mm-2-0-v34-k9wv3x`.

## CI coverage (master gate)

All CI jobs use `PURE_SOURCE_BUILD=1` (no rom4s / reference `.so` splice).

| Job | `sm13-mm20` in matrix? | Notes |
|-----|------------------------|-------|
| `build-linux` | yes | Builds package in `debian:11` |
| `test-built-debian` (`debian:11`) | yes | Primary smoke gate |
| `test-built-botplay-matrix` (`debian:11`) | yes | SMAC + SDKHooks OnTakeDamage stress |
| `test-built-debian` (`debian:latest`) | **no** | `sm13-dev` only |
| `test-built-rhel` (`rockylinux:9`) | **no** | `sm13-dev` only |
| `test-built-smoke` (host runner) | **no** | `sm13-dev` only |
| `test-built-botplay-extra` | **no** | `sm13-dev` only (workflow_dispatch opt-in) |

**Verified on every `master` push:** debian:11 smoke + botplay-stress for tag `1.13.0.7404-mm2.0.0`.

## Full pure-source recheck (branch `cursor/sm13-mm20-full-recheck`, 2026-09-12)

That branch (commit `745aef4`) ran a one-off CI expansion to prove `sm13-mm20` boots on the
**same distro surface as `sm13-dev`**, not only on debian:11. Findings:

### What passes (same as master gate)

- `debian:11` smoke — `sm version`, extensions (BinTools, SDK Tools, CS Tools, SDK Hooks), plugins
- `debian:11` botplay-stress — SMAC, SDKHook_OnTakeDamage hits, map rotation
- ABI check (`check-package.sh`) — glibc ≤2.29, no `__cxx11` / modern pthread symbols

### What the recheck branch added (not merged to master)

Workflow-only changes in `.github/workflows/test-server.yml`:

1. **`test-built-debian`**: add `sm13-mm20` rows for `debian:latest` and `rockylinux:9`
2. **`test-built-smoke`**: matrix over `sm13-dev` + `sm13-mm20` on the GitHub host runner
3. **`test-built-botplay-extra`**: matrix over both lines × debian:11 / debian:12 / rockylinux:9
4. **`workflow_dispatch` defaults**: `css34_line=sm13-mm20`, extra distros + bisect enabled

Intent: manual “recheck release tag” runs without touching rom4s/reference botplay jobs.

### Backlog (optional CI hardening)

If we want parity with `sm13-dev` extended coverage, port items 1–3 above onto current
`test-server.yml` (which now also includes `sm13-latest`). Low priority — debian:11 gate
already covers the released tag.

### Manual recheck today (without the deleted branch)

GitHub Actions → **Test Server** → **Run workflow**:

| Input | Value |
|-------|-------|
| `css34_line` | `sm13-mm20` |
| `run_full_botplay_matrix` | true |
| `botplay_extra_distros` | true (sm13-dev packages only unless workflow is extended) |
| `run_reference_botplay` | false |

Local:

```bash
CSS34_LINE=sm13-mm20 PURE_SOURCE_BUILD=1 builder/docker/legacy-build.sh
SM_PACKAGE=packages/sourcemod-*-css34-linux.tar.gz \
MM_PACKAGE=packages/mmsource-*-css34-linux.tar.gz \
  testing/scripts/smoke-test.sh
```

## Related lines

| Line | SM | MM | Branch / PR |
|------|----|----|-------------|
| `sm13-mm20` | 7404 | 2.0 git1407 | master (this doc) |
| `sm13-mm20-khook` | 7472+ | 2.0 git1469+ | PR #54 (`cursor/mm20-khook-c33d`) — header-graft experiment |
| KHook-native SM | KHook branch 7519 | 2.0 git1469 | `claude/khook-mm-2-0-v34-k9wv3x` — separate track |

Do not mix Metamod from one line with SourceMod from another. See [README.md](../README.md).
