diff --git a/AMBuildScript b/AMBuildScript
index 4d4eb2f9..476c5d7e 100644
--- a/AMBuildScript
+++ b/AMBuildScript
@@ -45,7 +45,7 @@ class SMConfig(object):
     self.extensions = []
     self.generated_headers = None
     self.mms_root = None
-    self.mysql_root = {}
+    self.mariadb_root = {}
     self.spcomp = None
     self.spcomp_bins = []
     self.smx_files = {}
@@ -140,28 +140,26 @@ class SMConfig(object):
 
     if builder.options.hasMySql:
       if 'x86' in self.target_archs:
-        if builder.options.mysql_path:
-          self.mysql_root['x86'] = builder.options.mysql_path
+        mariadb_path = getattr(builder.options, 'mariadb_path',
+                               getattr(builder.options, 'mysql_path', None))
+        if mariadb_path:
+          self.mariadb_root['x86'] = mariadb_path
         else:
-          for i in range(10):
-            self.mysql_root['x86'] = ResolveEnvPath('MYSQL57', 'mysql-5.' + str(i))
-            if self.mysql_root['x86']:
-              break
-        if not self.mysql_root['x86'] or not os.path.isdir(self.mysql_root['x86']):
-          raise Exception('Could not find a path to MySQL. Configure with --no-mysql to disable it.')
-        self.mysql_root['x86'] = Normalize(self.mysql_root['x86'])
+          self.mariadb_root['x86'] = ResolveEnvPath('MARIADB_CONNECTOR_C', 'mariadb-connector-c-x86')
+        if not self.mariadb_root['x86'] or not os.path.isdir(self.mariadb_root['x86']):
+          raise Exception('Could not find an x86 MariaDB Connector/C path. Configure with --no-mysql to disable it.')
+        self.mariadb_root['x86'] = Normalize(self.mariadb_root['x86'])
 
       if 'x86_64' in self.target_archs:
-        if builder.options.mysql64_path:
-          self.mysql_root['x86_64'] = builder.options.mysql64_path
+        mariadb64_path = getattr(builder.options, 'mariadb64_path',
+                                 getattr(builder.options, 'mysql64_path', None))
+        if mariadb64_path:
+          self.mariadb_root['x86_64'] = mariadb64_path
         else:
-          for i in range(10):
-            self.mysql_root['x86_64'] = ResolveEnvPath('MYSQL57_64', 'mysql-5.' + str(i) + '-x86_64')
-            if self.mysql_root['x86_64']:
-              break
-        if not self.mysql_root['x86_64'] or not os.path.isdir(self.mysql_root['x86_64']):
-          raise Exception('Could not find a path to 64-bit MySQL!')
-        self.mysql_root['x86_64'] = Normalize(self.mysql_root['x86_64'])
+          self.mariadb_root['x86_64'] = ResolveEnvPath('MARIADB_CONNECTOR_C_64', 'mariadb-connector-c-x86_64')
+        if not self.mariadb_root['x86_64'] or not os.path.isdir(self.mariadb_root['x86_64']):
+          raise Exception('Could not find an x86_64 MariaDB Connector/C path.')
+        self.mariadb_root['x86_64'] = Normalize(self.mariadb_root['x86_64'])
 
   def configure(self):
     builder.AddConfigureFile('pushbuild.txt')
@@ -272,7 +270,7 @@ class SMConfig(object):
       '-fvisibility=hidden',
     ]
 
-    cxx.cxxflags += ['-std=c++17']
+    cxx.cxxflags += ['-std=c++20']
 
     cxx.cxxflags += [
       '-fno-threadsafe-statics',
@@ -327,6 +325,13 @@ class SMConfig(object):
     if cxx.version >= 'clang-21.0':
         cxx.cflags += ['-Wno-uninitialized-const-pointer']
 
+    if cxx.version >= 'clang-16.0' or cxx.version >= 'apple-clang-15.0':
+        cxx.cxxflags += ['-Wno-deprecated-anon-enum-enum-conversion']
+
+    # C++20 deprecates volatile ++/-- and compound assignment (P1152R4).
+    if cxx.version >= 'clang-12.0' or cxx.version >= 'apple-clang-13.0':
+        cxx.cxxflags += ['-Wno-deprecated-volatile']
+
     if have_gcc:
       cxx.cflags += ['-Wno-maybe-uninitialized']
 
@@ -357,7 +362,7 @@ class SMConfig(object):
       '/EHsc',
       '/GR-',
       '/TP',
-      '/std:c++17',
+      '/std:c++20',
     ]
     cxx.linkflags += [
       'kernel32.lib',
@@ -414,6 +419,7 @@ class SMConfig(object):
     elif cxx.family == 'clang':
       cxx.linkflags += ['-lgcc_eh']
     cxx.linkflags += ['-static-libstdc++']
+    cxx.linkflags += ['-Wl,--exclude-libs=libstdc++.a']
 
   def configure_mac(self, cxx):
     cxx.defines += ['OSX', '_OSX', 'POSIX', 'KE_ABSOLUTELY_NO_STL']
@@ -436,19 +442,34 @@ class SMConfig(object):
         return lambda builder, name: self.StaticLibrary(builder, cxx, name)
       extra_vars = {'Configure': get_configure_fn(cxx)}
       libamtl = builder.Build('public/amtl/amtl/AMBuilder', extra_vars)
-      self.libamtl[cxx.target.arch] = libamtl.binary
+      self.libamtl[cxx.target] = libamtl.binary
+
+  def BinaryOutputName(self, binary, binary_type):
+    target = binary.compiler.target
+    if binary_type == 'program':
+      name_fn = binary.compiler.vendor.nameForExecutable
+    elif binary_type == 'library':
+      name_fn = binary.compiler.vendor.nameForSharedLibrary
+    else:
+      raise Exception('Unsupported binary type: {}'.format(binary_type))
 
-  def AddVersioning(self, binary):
+    try:
+      return name_fn(binary.name_, target)
+    except TypeError:
+      # Older AMBuild versions did not require the target argument.
+      return name_fn(binary.name_)
+
+  def AddVersioning(self, binary, binary_type):
     if binary.compiler.target.platform == 'windows':
       binary.sources += ['version.rc']
       binary.compiler.rcdefines += [
-        'BINARY_NAME="{0}"'.format(binary.outputFile),
+        'BINARY_NAME="{0}"'.format(self.BinaryOutputName(binary, binary_type)),
         'RC_COMPILE',
       ]
       if self.use_auto_versioning():
         binary.compiler.rcdefines += ['SM_GENERATED_BUILD']
     elif binary.compiler.target.platform == 'mac':
-      if binary.type == 'library':
+      if binary_type == 'library':
         binary.compiler.postlink += [
           '-compatibility_version', '1.0.0',
           '-current_version', self.productVersion
@@ -460,7 +481,7 @@ class SMConfig(object):
     
   def LibraryBuilder(self, compiler, name):
     binary = compiler.Library(name)
-    self.AddVersioning(binary)
+    self.AddVersioning(binary, 'library')
     if binary.compiler.like('msvc'):
       binary.compiler.linkflags += ['/SUBSYSTEM:WINDOWS']
     self.AddCxxCompat(binary)
@@ -475,7 +496,7 @@ class SMConfig(object):
 
   def ProgramBuilder(self, compiler, name):
     binary = compiler.Program(name)
-    self.AddVersioning(binary)
+    self.AddVersioning(binary, 'program')
     if '-static-libgcc' in binary.compiler.linkflags:
       binary.compiler.linkflags.remove('-static-libgcc')
     if self.enable_asan:
@@ -554,14 +575,14 @@ class SMConfig(object):
     binary = project.Configure(compiler, name,
                                '{0} - {1} {2}'.format(self.tag, sdk['name'], compiler.target.arch))
     self.AddCxxCompat(binary)
-    self.AddVersioning(binary)
+    self.AddVersioning(binary, 'library')
     return self.ConfigureForHL2(context, binary, sdk)
 
   def HL2ExtConfig(self, project, context, compiler, name, sdk):
     binary = project.Configure(compiler, name,
                                '{0} - {1} {2}'.format(self.tag, sdk['name'], compiler.target.arch))
     self.AddCxxCompat(binary)
-    self.AddVersioning(binary)
+    self.AddVersioning(binary, 'library')
     self.ConfigureForHL2(context, binary, sdk)
     self.ConfigureForExtension(context, binary.compiler)
     return binary
@@ -642,6 +663,9 @@ for spcomp in SP.spcomp:
         SM.spcomp = spcomp
     SM.spcomp_bins.append(spcomp)
 
+for spcomp in SP.oldspcomp:
+    SM.spcomp_bins.append(spcomp)
+
 # If we have a universal binary, ignore all other spcomps.
 if SM.spcomp.target.arch == 'universal':
     SM.spcomp_bins = [SM.spcomp]
diff --git a/core/CoreConfig.cpp b/core/CoreConfig.cpp
index 1f944abf..611584f5 100644
--- a/core/CoreConfig.cpp
+++ b/core/CoreConfig.cpp
@@ -683,7 +683,7 @@ bool SM_ExecuteConfig(IPlugin *pl, AutoConfig *cfg, bool can_create)
 		return can_create;
 	}
 
-	std::list<const ConVar *> *convars = nullptr;
+	std::list<const ConVar *> *convars = NULL;
 	if (!pl->GetProperty("ConVarList", (void **)&convars, false) || !convars)
 	{
 		if (file_exists)
diff --git a/core/EventManager.cpp b/core/EventManager.cpp
index bf453703..8f065419 100644
--- a/core/EventManager.cpp
+++ b/core/EventManager.cpp
@@ -63,9 +63,8 @@ EventManager::EventManager() : m_EventType(0),
 EventManager::~EventManager()
 {
 	/* Free memory used by EventInfo structs if any */
-	while (!m_FreeEvents.empty())
-	{
-		delete (m_FreeEvents.top());
+	while (!m_FreeEvents.empty()) {
+		delete m_FreeEvents.top();
 		m_FreeEvents.pop();
 	}
 }
@@ -301,11 +300,7 @@ EventHookError EventManager::UnhookEvent(const char *name, IPluginFunction *pFun
 		}
 
 		/* Make sure the event was actually being hooked */
-		auto iter = pHookList->begin();
-		while (iter != pHookList->end() && *iter != pHook) {
-			iter++;
-		}
-		if (iter == pHookList->end())
+		if (std::find(pHookList->begin(), pHookList->end(), pHook) == pHookList->end())
 		{
 			return EventHookErr_NotActive;
 		}
diff --git a/core/GameHooks.h b/core/GameHooks.h
index 4a6b92e0..260fa7ba 100644
--- a/core/GameHooks.h
+++ b/core/GameHooks.h
@@ -34,7 +34,7 @@
 #include <iserverplugin.h>
 #include <amtl/am-refcounting.h>
 #include <amtl/am-vector.h>
-#include <amtl/am-function.h>
+#include <functional>
 #include <khook.hpp>
 
 class ConVar;
@@ -64,7 +64,7 @@ class CommandHook : public ke::Refcounted<CommandHook>
 {
 public:
 	// return false to RETURN_META(MRES_IGNORED), or true to SUPERCEDE.
-	typedef ke::Function<bool(int, const ICommandArgs *)> Callback;
+	typedef std::function<bool(int, const ICommandArgs *)> Callback;
 
 public:
 	CommandHook(ConCommand *cmd, const Callback &callback, bool post);
diff --git a/core/NextMap.cpp b/core/NextMap.cpp
index c0c56565..87844346 100644
--- a/core/NextMap.cpp
+++ b/core/NextMap.cpp
@@ -201,6 +201,7 @@ NextMapManager::NextMapManager() :
 	m_HookChangeLevel(&IVEngineServer::ChangeLevel, this, &NextMapManager::HookChangeLevel, nullptr)
 {
 	m_tempChangeInfo = MapChangeData();
+	m_mapHistory = std::list<MapChangeData *>();
 }
 
 #if SOURCE_ENGINE >= SE_ORANGEBOX
diff --git a/core/PlayerManager.cpp b/core/PlayerManager.cpp
index 3599d732..2bdf95ea 100644
--- a/core/PlayerManager.cpp
+++ b/core/PlayerManager.cpp
@@ -701,7 +701,6 @@ KHook::Return<void> PlayerManager::OnClientPutInServer(IServerGameClients* clien
 			/* :TODO: kick the bot if it's rejected */
 			return { KHook::Action::Ignore };
 		}
-
 		IClientListener *pListener = NULL;
 		for (auto iter=m_hooks.begin(); iter!=m_hooks.end(); iter++)
 		{
diff --git a/core/UserMessages.cpp b/core/UserMessages.cpp
index e7f4a688..1892c3df 100644
--- a/core/UserMessages.cpp
+++ b/core/UserMessages.cpp
@@ -450,7 +450,6 @@ bool UserMessages::InternalUnhook(int msg_id, IBitBufUserMessageListener *pListe
 #endif
 {
 	MsgList *pList;
-	MsgIter iter;
 	ListenerInfo *pInfo;
 	bool deleted = false;
 
@@ -460,7 +459,7 @@ bool UserMessages::InternalUnhook(int msg_id, IBitBufUserMessageListener *pListe
 	}
 
 	pList = (intercept) ? &m_msgIntercepts[msg_id] : &m_msgHooks[msg_id];
-	for (iter=pList->begin(); iter!=pList->end(); iter++)
+	for (auto iter=pList->begin(); iter!=pList->end(); iter++)
 	{
 		pInfo = (*iter);
 		if (pInfo->Callback == pListener && pInfo->IsNew == isNew)
@@ -628,13 +627,12 @@ KHook::Return<void> UserMessages::OnMessageEnd_Post(IVEngineServer*)
 	}
 
 	MsgList *pList;
-	MsgIter iter;
 	ListenerInfo *pInfo;
 
 	m_InHook = false;
 
 	pList = &m_msgIntercepts[m_CurId];
-	for (iter=pList->begin(); iter!=pList->end(); )
+	for (auto iter=pList->begin(); iter!=pList->end(); )
 	{
 		pInfo = (*iter);
 		if (m_BlockEndPost && !pInfo->IsNew)
@@ -661,7 +659,7 @@ KHook::Return<void> UserMessages::OnMessageEnd_Post(IVEngineServer*)
 	}
 
 	pList = &m_msgHooks[m_CurId];
-	for (iter=pList->begin(); iter!=pList->end(); )
+	for (auto iter=pList->begin(); iter!=pList->end(); )
 	{
 		pInfo = (*iter);
 		if (m_BlockEndPost && !pInfo->IsNew)
@@ -698,7 +696,6 @@ KHook::Return<void> UserMessages::OnMessageEnd_Pre(IVEngineServer*)
 	}
 
 	MsgList *pList;
-	MsgIter iter;
 	ListenerInfo *pInfo;
 
 	ResultType res;
@@ -706,7 +703,7 @@ KHook::Return<void> UserMessages::OnMessageEnd_Pre(IVEngineServer*)
 	bool handled = false;
 
 	pList = &m_msgIntercepts[m_CurId];
-	for (iter=pList->begin(); iter!=pList->end(); )
+	for (auto iter=pList->begin(); iter!=pList->end(); )
 	{
 		pInfo = (*iter);
 		pInfo->IsHooked = true;
@@ -790,7 +787,7 @@ KHook::Return<void> UserMessages::OnMessageEnd_Pre(IVEngineServer*)
 #endif
 
 		pList = &m_msgHooks[m_CurId];
-		for (iter=pList->begin(); iter!=pList->end(); )
+		for (auto iter=pList->begin(); iter!=pList->end(); )
 		{
 			pInfo = (*iter);
 			pInfo->IsHooked = true;
diff --git a/core/UserMessages.h b/core/UserMessages.h
index 2b511765..c1c2aa30 100644
--- a/core/UserMessages.h
+++ b/core/UserMessages.h
@@ -72,7 +72,6 @@ struct ListenerInfo
 };
 
 typedef std::list<ListenerInfo *> MsgList;
-typedef MsgList::iterator MsgIter;
 
 class UserMessages : 
 	public IUserMessages,
diff --git a/core/logic/AMBuilder b/core/logic/AMBuilder
index c4e863db..e8738c85 100644
--- a/core/logic/AMBuilder
+++ b/core/logic/AMBuilder
@@ -8,6 +8,8 @@ for cxx in builder.targets:
     os.path.join(builder.sourcePath, 'core', 'logic'),
     os.path.join(builder.sourcePath, 'public'),
     os.path.join(builder.sourcePath, 'sourcepawn', 'include'),
+    os.path.join(builder.sourcePath, 'sourcepawn'),
+    os.path.join(builder.sourcePath, 'sourcepawn', 'vm'),
     os.path.join(builder.sourcePath, 'public', 'amtl', 'amtl'),
     os.path.join(builder.sourcePath, 'public', 'amtl'),
     os.path.join(SM.mms_root, 'third_party', 'khook', 'include')
@@ -24,11 +26,12 @@ for cxx in builder.targets:
     binary.compiler.cflags += ['-Wno-deprecated-declarations']
     binary.compiler.postlink += ['-framework', 'CoreServices', '-lm']
 
-  arch = binary.compiler.target.arch
+  target = binary.compiler.target
   binary.compiler.linkflags += [
-    SP.static_libsp[arch],
-    SP.libamtl[arch],
-    SP.zlib[arch],
+    SP.static_libsp[target],
+    SP.libamtl[target],
+    SP.zlib[target],
+    SP.mimalloc[target],
   ]
 
   if binary.compiler.family == 'gcc' or binary.compiler.family == 'clang':
diff --git a/core/logic/smn_core.cpp b/core/logic/smn_core.cpp
index 57efb19e..fcf63874 100644
--- a/core/logic/smn_core.cpp
+++ b/core/logic/smn_core.cpp
@@ -59,6 +59,7 @@
 #include <bridge/include/CoreProvider.h>
 #include <bridge/include/IScriptManager.h>
 #include <bridge/include/IExtensionBridge.h>
+#include <vector>
 
 using namespace SourceMod;
 using namespace SourcePawn;
diff --git a/core/smn_console.cpp b/core/smn_console.cpp
index d288acee..103a38a8 100644
--- a/core/smn_console.cpp
+++ b/core/smn_console.cpp
@@ -1515,6 +1515,7 @@ static cell_t sm_CommandIteratorPlugin(IPluginContext *pContext, const cell_t *p
 	{
 		return pContext->ThrowNativeError("Invalid CommandIterator Handle %x", params[1]);
 	}
+
 	const std::list<ConCmdInfo *> &cmds = g_ConCmds.GetCommandList();
 	if (!iter->started || iter->iter == cmds.end())
 	{
diff --git a/core/sourcemod.cpp b/core/sourcemod.cpp
index ee1235b0..a06a5a36 100644
--- a/core/sourcemod.cpp
+++ b/core/sourcemod.cpp
@@ -52,8 +52,9 @@
 /**/
 
 SourceModBase g_SourceMod;
+
 std::string g_BaseDir;
-SourcePawn::ISourcePawnEnvironment *g_pPawnEnv = NULL;
+ISourcePawnEnvironment *g_pPawnEnv = NULL;
 IdentityToken_t *g_pCoreIdent = NULL;
 IForward *g_pOnMapInit = nullptr;
 IForward *g_pOnMapEnd = nullptr;
diff --git a/extensions/sdkhooks/extension.cpp b/extensions/sdkhooks/extension.cpp
index 3ff82c7c..f9047ec9 100644
--- a/extensions/sdkhooks/extension.cpp
+++ b/extensions/sdkhooks/extension.cpp
@@ -419,11 +419,11 @@ FeatureStatus SDKHooks::GetFeatureStatus(FeatureType type, const char *name)
 
 static void PopulateCallbackList(const std::vector<HookList> &source, std::vector<IPluginFunction *> &destination, int entity)
 {
-	if (source.empty())
-	{
+	// SourcePawn cannot be called from worker threads. This can happen on some
+	// engine variants, where the engine invokes ShouldCollide off-thread.
+	if (g_MainThreadId != std::this_thread::get_id())
 		return;
-	}
-
+	
 	destination.reserve(8);
 	for (size_t iter = 0; iter < source.size(); ++iter)
 	{
diff --git a/extensions/sdktools/AMBuilder b/extensions/sdktools/AMBuilder
index 2b6bc35c..8e24001d 100644
--- a/extensions/sdktools/AMBuilder
+++ b/extensions/sdktools/AMBuilder
@@ -29,8 +29,6 @@ project.sources += [
 
 for sdk_name in SM.sdks:
   sdk = SM.sdks[sdk_name]
-  if sdk['name'] in ['mock']:
-    continue
 
   for cxx in builder.targets:
     if not cxx.target.arch in sdk['platforms'][cxx.target.platform]:
diff --git a/extensions/sdktools/output.cpp b/extensions/sdktools/output.cpp
index ee3f4399..c8d51c16 100644
--- a/extensions/sdktools/output.cpp
+++ b/extensions/sdktools/output.cpp
@@ -33,6 +33,8 @@
 #include "output.h"
 #include "am-string.h"
 
+#include <list>
+
 ISourcePawnEngine *spengine = NULL;
 EntityOutputManager g_OutputManager;
 
@@ -386,4 +388,4 @@ const char *EntityOutputManager::FindOutputName(void *pOutput, CBaseEntity *pAct
 	}
 
 	return NULL;
-}
\ No newline at end of file
+}
\ No newline at end of file
diff --git a/extensions/sdktools/tenatives.cpp b/extensions/sdktools/tenatives.cpp
index ea1327c0..b1be092e 100644
--- a/extensions/sdktools/tenatives.cpp
+++ b/extensions/sdktools/tenatives.cpp
@@ -178,8 +178,7 @@ bool TempEntHooks::RemoveHook(const char *name, IPluginFunction *pFunc)
 
 	if (m_TEHooks->Retrieve(name, reinterpret_cast<void **>(&pInfo)))
 	{
-		auto iter = pInfo->lst.begin();
-		while (iter != pInfo->lst.end() && *iter != pFunc) { iter++; };
+		auto iter = std::find(pInfo->lst.begin(), pInfo->lst.end(), pFunc);
 		if (iter != pInfo->lst.end())
 		{
 			pInfo->lst.erase(iter);
diff --git a/extensions/sdktools/vsound.cpp b/extensions/sdktools/vsound.cpp
index d9ca56fc..d7a58756 100644
--- a/extensions/sdktools/vsound.cpp
+++ b/extensions/sdktools/vsound.cpp
@@ -128,12 +128,11 @@ void SoundHooks::Shutdown()
 
 void SoundHooks::OnPluginUnloaded(IPlugin *plugin)
 {
-	SoundHookIter iter;
 	IPluginContext *pContext = plugin->GetBaseContext();
 
 	if (m_AmbientCount)
 	{
-		for (iter=m_AmbientFuncs.begin(); iter!=m_AmbientFuncs.end(); )
+		for (auto iter=m_AmbientFuncs.begin(); iter!=m_AmbientFuncs.end(); )
 		{
 			if ((*iter)->GetParentContext() == pContext)
 			{
@@ -148,7 +147,7 @@ void SoundHooks::OnPluginUnloaded(IPlugin *plugin)
 	}
 	if (m_NormalCount)
 	{
-		for (iter=m_NormalFuncs.begin(); iter!=m_NormalFuncs.end(); )
+		for (auto iter=m_NormalFuncs.begin(); iter!=m_NormalFuncs.end(); )
 		{
 			if ((*iter)->GetParentContext() == pContext)
 			{
@@ -181,8 +180,7 @@ bool SoundHooks::RemoveHook(int type, IPluginFunction *pFunc)
 {
 	if (type == NORMAL_SOUND_HOOK)
 	{
-		auto iter =	m_NormalFuncs.begin();
-		while (iter != m_NormalFuncs.end() && *iter != pFunc) { iter++; }
+		auto iter = std::find(m_NormalFuncs.begin(), m_NormalFuncs.end(), pFunc);
 		if (iter != m_NormalFuncs.end())
 		{
 			m_NormalFuncs.erase(iter);
@@ -196,8 +194,7 @@ bool SoundHooks::RemoveHook(int type, IPluginFunction *pFunc)
 	}
 	else if (type == AMBIENT_SOUND_HOOK)
 	{
-		auto iter =	m_AmbientFuncs.begin();
-		while (iter != m_AmbientFuncs.end() && *iter != pFunc) { iter++; }
+		auto iter = std::find(m_AmbientFuncs.begin(), m_AmbientFuncs.end(), pFunc);
 		if (iter != m_AmbientFuncs.end())
 		{
 			m_AmbientFuncs.erase(iter);
@@ -216,14 +213,13 @@ bool SoundHooks::RemoveHook(int type, IPluginFunction *pFunc)
 KHook::Return<void> SoundHooks::OnEmitAmbientSound(IVEngineServer* this_ptr, int entindex, const Vector &pos, const char *samp, float vol, 
 									soundlevel_t soundlevel, int fFlags, int pitch, float delay)
 {
-	SoundHookIter iter;
 	IPluginFunction *pFunc;
 	cell_t vec[3] = {sp_ftoc(pos.x), sp_ftoc(pos.y), sp_ftoc(pos.z)};
 	cell_t res = static_cast<ResultType>(Pl_Continue);
 	char buffer[PLATFORM_MAX_PATH];
 	ke::SafeStrcpy(buffer, sizeof(buffer), samp);
 
-	for (iter=m_AmbientFuncs.begin(); iter!=m_AmbientFuncs.end(); iter++)
+	for (auto iter=m_AmbientFuncs.begin(); iter!=m_AmbientFuncs.end(); iter++)
 	{
 		pFunc = (*iter);
 		pFunc->PushStringEx(buffer, sizeof(buffer), SM_PARAM_STRING_COPY, SM_PARAM_COPYBACK);
@@ -332,7 +328,6 @@ KHook::Return<void> SoundHooks::OnEmitSound(IEngineSound* this_ptr, IRecipientFi
 							 float soundtime, int speakerentity)
 #endif
 {
-	SoundHookIter iter;
 	IPluginFunction *pFunc;
 	cell_t res = static_cast<ResultType>(Pl_Continue);
 	char buffer[PLATFORM_MAX_PATH];
@@ -347,7 +342,7 @@ KHook::Return<void> SoundHooks::OnEmitSound(IEngineSound* this_ptr, IRecipientFi
 	int nSeed = 0;
 #endif
 
-	for (iter=m_NormalFuncs.begin(); iter!=m_NormalFuncs.end(); iter++)
+	for (auto iter=m_NormalFuncs.begin(); iter!=m_NormalFuncs.end(); iter++)
 	{
 		int players[SM_MAXPLAYERS], size;
 		size = _FillInPlayers(players, &filter);
@@ -484,7 +479,6 @@ KHook::Return<void> SoundHooks::OnEmitSound2(IEngineSound* this_ptr, IRecipientF
 							 float soundtime, int speakerentity)
 #endif
 {
-	SoundHookIter iter;
 	IPluginFunction *pFunc;
 	cell_t res = static_cast<ResultType>(Pl_Continue);
 	cell_t sndlevel = static_cast<cell_t>(ATTN_TO_SNDLVL(flAttenuation));
@@ -500,7 +494,7 @@ KHook::Return<void> SoundHooks::OnEmitSound2(IEngineSound* this_ptr, IRecipientF
 	int nSeed = 0;
 #endif
 
-	for (iter=m_NormalFuncs.begin(); iter!=m_NormalFuncs.end(); iter++)
+	for (auto iter=m_NormalFuncs.begin(); iter!=m_NormalFuncs.end(); iter++)
 	{
 		int players[SM_MAXPLAYERS], size;
 		size = _FillInPlayers(players, &filter);
diff --git a/extensions/sdktools/vsound.h b/extensions/sdktools/vsound.h
index f8526153..f7f969e4 100644
--- a/extensions/sdktools/vsound.h
+++ b/extensions/sdktools/vsound.h
@@ -39,8 +39,6 @@
 #define NORMAL_SOUND_HOOK		0
 #define AMBIENT_SOUND_HOOK		1
 
-typedef std::list<IPluginFunction *>::iterator SoundHookIter;
-
 class SoundHooks : public IPluginsListener
 {
 public: //IPluginsListener
